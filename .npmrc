# Skip puppeteer downloading Chromium during npm install
# (Chromium is not used in production - the browser.ts utility is legacy code)
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true
PUPPETEER_SKIP_DOWNLOAD=true
